import "../dist/index.js";

module.exports = {
    starknet: {
        network: "integrated-devnet"
    },
    networks: {
        integratedDevnet: {
            dockerizedVersion: "latest"
        }
    }
};
